#include <iostream>
int main()
{
	system("pause");
}
/*
#include <iostream>
#include <string.h>
#include "Wallet.h"
#include "Order.h"
#include "ID.h"
#include "HelperFunctions.h"

using std::cin;
using std::cout;
using std::endl;

int main() {
	initilizeID();
	char command[32];
	while (true) {
		cin >> command;
		if (!strcmp(command, "add-wallet")) {
			addWallet();
		}
		else if (!strcmp(command, "make-order")) {
			createOrder();
		}
		else if (!strcmp(command, "wallet-info")) {

		}
		else if (!strcmp(command, "attract-ivestors")) {

		}
		else if (!strcmp(command, "quit")) {
			break;
		}
		else {
			cout << "Invalid command, enter a new one: " << endl;
		}
	}
	saveLastID();
	return 0;
}*/